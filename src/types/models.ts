export type userData = {
    name: string;
    password: string;
    mail: string;
    balance: number;
};

export type serverData = {
    name: string;
    price: number;
    ip: string;
    userId: number;
};
